import { Layout } from "../common/Layout/Layout";
import { MovieDetails } from "../modules/Movies/MovieDetails";

export const Movie = () => {
  return (
    <Layout>
      <MovieDetails />
    </Layout>
  );
};
